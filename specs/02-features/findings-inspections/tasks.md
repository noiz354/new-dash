# Findings & Inspections — Tasks (From SDD/TODO)

- [x] Findings CRUD + convert→WO (GAP-10 = REPORTED; "P1S3" nol di `docs/`;
  TANPA `inspection-templates` API)
- [x] Inspection execution + schedule (klaim "templates" = REPORTED)
- [ ] Sisa Tier-4 terkait inspeksi; tulis E2E khusus findings (belum ada)
